POST /auth/signup
POST /auth/login

POST /create

GET /{short_url} (this would have rate limited access)
GET /urls (list all urls for the user)
GET /{short_url}/metrics (only accessible to the creator/admin)


Access patterns

PUT user details
GET user by email

PUT url against shorturl
GET url by shorturl

PUT metrics by shorturl
GET metrics by shorturl
